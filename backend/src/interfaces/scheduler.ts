export interface IPlayerTurnTimer {
  timer: number;
  jobId: string;
  tableId: string;
  userId: string;
}

export interface IScoreBoardTimer {
  timer: number;
  jobId: string;
  tableId: string;
}
